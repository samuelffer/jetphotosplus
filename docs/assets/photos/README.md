# Aircraft photos

Put `.jpg`, `.jpeg`, `.png`, or `.webp` aircraft photos in this folder.
Then double-click `update_photos.bat` in the root folder.

It generates `assets/photos.js` automatically. The slideshow then works:
- offline;
- by opening `index.html` directly;
- on GitHub Pages.

No Node.js, Python, server, or GitHub Action is required.
